import { StyleSheet, Text, View } from "react-native";
import React from "react";

const two = () => {
	return (
		<View>
			<Text>two</Text>
		</View>
	);
};

export default two;

const styles = StyleSheet.create({});
